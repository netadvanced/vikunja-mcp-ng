# Roadmap & Status

This document is the durable record of **where this project is going, where it stands, and why** — the vision, the locked architecture decisions, and the honest state of what's implemented, what isn't, and what's next.

Companion documents:
- **[Tracking issue #28](https://github.com/netadvanced/vikunja-mcp-ng/issues/28)** — the live working checklist (waves, PRs, checkboxes). This roadmap explains *why*; #28 tracks *progress*.
- **[docs/API-COVERAGE.md](API-COVERAGE.md)** — the raw endpoint-by-endpoint audit this plan was built from. Row-by-row re-verified 2026-07-18 post node-vikunja removal; the denominator/header counts were independently re-confirmed 2026-07-21 against the vendored Vikunja 2.4.0 spec (item T2, decision 19) — zero new/removed operations, counts unchanged. **Read §4's 2026-08-31 notes before quoting that:** the comparison's "2.3.0-era" side was an unstable build well past the `v2.3.0` tag, so the 169 denominator describes the 2.4.0 surface, not the 2.3.0 floor. See §4.
- **[docs/API_NOTES.md](API_NOTES.md)** / **[docs/VIKUNJA_API_ISSUES.md](VIKUNJA_API_ISSUES.md)** — hard-won implementation gotchas and known upstream API quirks. Read before touching endpoint code.
- **[docs/ENDPOINT-PLAYBOOK.md](ENDPOINT-PLAYBOOK.md)** — the how-to conventions for implementing new capabilities.
- **[docs/ENDPOINT-TAIL-RETRIAGE.md](ENDPOINT-TAIL-RETRIAGE.md)** — the C2 (2026-07-18) re-examination of all 64 not-implemented ops under direct REST, with the grouped IMPLEMENT backlog (see §4).
- **[docs/OIDC-RESOURCE-SERVER.md](OIDC-RESOURCE-SERVER.md)** — the locked design (owner review 2026-07-20) for the multi-user OIDC resource-server mode, plus a record of where the shipped code diverged from it. Waves H1+H2 **merged to `main` 2026-08-14** (#216, #221) and shipped in `0.7.0-beta.0`/`.1` — see §2 and §6. **[docs/OIDC-SETUP.md](OIDC-SETUP.md)** is the operator-facing install and configuration manual.
- **[docs/RELEASING.md](RELEASING.md)** — the release checklist and the current tag-triggered publish pipeline; this roadmap tracks *whether* and *when* to release, RELEASING.md covers *how*.

---

## 1. Vision — where we want to end up

An **AI-first MCP server for Vikunja**: not a 1:1 REST proxy, but a set of task-level tools an AI assistant can use reliably.

**Design pillars (all decided 2026-07-17, see §3):**

1. **Composite-first tools.** One MCP call may perform several API calls with logic between them: verify-then-apply, resolve names to IDs internally, create-if-missing (idempotent "ensure" semantics), read-composites that answer in one call what would otherwise take four. The OpenAPI spec is our *coverage checklist*, not our tool design. Primitives (plain CRUD) remain available as subcommands where fine-grained control matters.
2. **Direct REST on generated types; `node-vikunja` is end-of-life.** The npm client library this project was originally built on was frozen (v0.4.0, May 2025) and had confirmed drift from the real API — stale field names, wrong endpoints, missing coverage. All HTTP now goes through our own thin REST layer (`src/utils/vikunja-rest.ts`) with TypeScript types **generated from the vendored OpenAPI spec**. The library was removed domain-by-domain across Wave D; the final PR (`waveD-node-vikunja-removal`, #73) migrated the last runtime call sites and type-only imports and **deleted the dependency** — zero occurrences in `package.json` and `package-lock.json`, and zero *code* references in `src/` (re-verified 2026-08-31; still zero hits in `package.json`/`package-lock.json`, and the only two hits under `src/` are historical explanatory comments in `src/tools/tasks/bulk-operations-simplified.ts` and `src/utils/error-handler.ts`, deliberately kept because they explain why a workaround exists), proven in CI-equivalent local gates.
3. **Deployable and safe by configuration.** A layered config (defaults → `vikunja-mcp.config.json` → env vars, env wins) enables/disables whole modules at tool-registration time — disabled modules are invisible to the AI client. Dangerous surfaces (admin, user deletion, token management) are **deny-by-default**. Secrets never live in the config file: every sensitive env var has a `*_FILE` variant for Docker Swarm secrets / Kubernetes mounts. Container packaging (multi-stage Dockerfile + compose example) landed in Wave E alongside the `vikunja-mcp-ng` repackaging.
4. **Honest semantics.** Vikunja has no transactions; our composites never pretend otherwise. A saga helper (`CompositeOperation`) provides opt-in best-effort rollback with compensations and full trace reporting; partial success is always reported explicitly. Tools that can't do something (deliver binary exports over MCP, persist templates across restarts) say so in their descriptions instead of pretending.
5. **Quality gates that mean something.** Every change lands as a PR with lint/typecheck/tests and a zero-net-new-regressions proof. The test suite reached fully-honest status in the 2026-07 repair waves; coverage thresholds are ratcheted to honest values and raised as coverage grows.

## 2. Current state (2026-08-31)

**`0.7.0-beta.1` is the current build and npm's `beta` tag; `0.6.2` is still npm `latest`; the OIDC resource-server epic is merged to `main` and shipped in that beta; and the v2 API migration has moved out of `0.7.0` to `0.8.0` (issue [#184](https://github.com/netadvanced/vikunja-mcp-ng/issues/184)).** In sequence (full detail in §6; this is the summary):

- **Waves A–E (2026-07-17/18)** — baseline test-suite repair, the 16-bug compliance fix pass, build-out infrastructure (vendored spec/generated types/REST helper/saga helper/module config), the three-batch capability build-out with full `node-vikunja` migration and removal (decision 2, §3, fully executed — no dependency and no code references remain; see §1 pillar 2 for the exact re-verified wording), and repackaging as `vikunja-mcp-ng` with Docker distribution.
- **Wave F (2026-07-18)** — docs pass (this roadmap's prior rewrite, evaluated-options record, competitive-review snapshot), read-only safety mode + tool annotations, subtask composites, templates file persistence, dependency/security pass, sample screenshots. **Done.**
- **Wave G-endpoint-tail (2026-07-18)** — the C2 re-triage's 20 approved-to-build operations (candidates G1–G7: CalDAV tokens, user-export status, avatar provider, user self-deletion, user-level webhooks, project backgrounds, task duplicate/mark-read) all shipped, flipping coverage 103→123 ✅ / 64→44 ❌. **Done.**
- **Release engineering + releases through `0.6.2` (2026-07-18 → 2026-07-28)** — SemVer/changelog discipline landed, then **releases `0.1.0` through `0.6.2` shipped** (see `CHANGELOG.md`); the tag-triggered `.github/workflows/release.yml` was installed 2026-07-20 (OIDC Trusted Publishing, zero tokens/secrets, automatic provenance) and **`0.5.1` was the first release published through it**, alongside the GHCR compat-tagged Docker image. **`0.6.0` (2026-07-24)** is the announced *"optimised for Vikunja 2.4"* reliability/agent-ergonomics milestone (fixed a circuit-breaker cascade and silent date data-loss, plus Kanban/filter ergonomics for weaker agents) and was the first multi-arch GHCR release (`linux/amd64` + `linux/arm64`, SLSA provenance, #146) — though multi-arch only became *reliable* with #204's native-runner rebuild (see below). **`0.6.2` (2026-07-28) is still npm's `latest`** — re-verified 2026-08-31: `npm view vikunja-mcp-ng dist-tags` → `latest: 0.6.2`, `beta: 0.7.0-beta.1`. See `docs/RELEASING.md` and decision 20 (§3).
- **Vikunja 2.4.0 alignment (item A1, 2026-07-20)** — the e2e default pin and vendored spec moved `2.3.0` → `2.4.0`; **policy: minimum supported Vikunja is 2.3.0 (v1-floor), aligned/tested default is 2.4.0.** Proof matrix green on both DB backends at both versions. **Done** (decision 18, §3). **Superseded on the floor, 2026-08-31:** the floor was raised 2.3.0 → 2.4.0 (decision 27), so floor and aligned now coincide at 2.4.0; the aligned half of decision 18 stands unchanged. See the 2.5.0/2.6.0 bullet below.
- **2.4.0 coverage re-audit (item T2, 2026-07-21)** — a method-level operation-set diff confirmed **zero new/removed operations** between the spec the audit was denominated against and the 2.4.0 spec, so the 169-operation denominator and the 123/1/1/44 header accounting are unchanged. **Done** (decision 19, §3). **Re-checked and clarified 2026-08-31:** that diff is correct as executed, but its "2.3.0-era" side is *not* the released `v2.3.0` tag — it is `v2.3.0-1019-g95b7e673`, a `try.vikunja.io` **unstable build 1019 commits past the tag**, which already contained the nine operations a fresh tag-to-tag `v2.3.0`→`v2.4.0` diff attributes to 2.4.0. Consequence worth knowing: **the 169 denominator has never been the released-2.3.0 surface** (that is 160 ops), and nine `✅` rows — the eight `/admin/*` operations and `GET /projects/{project}/tasks/by-index/{index}` — were implemented against operations a real 2.3.0 server does not have, even though 2.3.0 was then our declared floor. **Resolved 2026-08-31 by raising the floor to 2.4.0** (decision 27): the denominator and the floor now describe the same server surface. See the amendments to decisions 18 and 19 in §3 and the note in §4.
- **Test-infra tightening (item T4, 2026-07-21)** — root-caused and fixed a `spyOn`/`mockRestore` interaction and silenced Web Storage warning noise in the suite. **Done.**
- **`0.6.1` — ergonomics/bugfix (shipped 2026-07-25)** — issue [#173](https://github.com/netadvanced/vikunja-mcp-ng/issues/173)'s `setup-kanban` composite landed (#175), cutting the Kanban-provisioning flow from ~38 tool calls to one (measured: haiku pass-rate 2/3 → 3/3 on `q3-offsite-kanban`), plus multi-task `apply-label`/`remove-label` (#178) and two live-server-only bugs in the new composite (bucket ordering #177, fail-fast on unknown columns #176). Shipped as a **patch** by owner discretion. See decision 21 (§3) and `CHANGELOG.md`.
- **`0.6.2` — correctness (shipped 2026-07-28)** — a JWT-authenticated second e2e session (#198) closed the coverage hole over JWT-only tools and immediately surfaced a real bug: cached circuit breakers were reused across differently-shaped operations, so **multipart uploads were fired through the JSON helper** (#199, affected `vikunja_tasks attach` and `vikunja_users upload-avatar`). Also: the MCP `initialize` handshake had reported a hardcoded `0.3.0` since `0.4.0` and is now derived from `package.json` with a live drift check (#186); `npm run build` now cleans `dist/` (#187); `setup-kanban`'s `columns` became optional (#185). Also a patch by owner discretion.
- **Release-pipeline repair (#204, 2026-07-28)** — emulated (QEMU) arm64 image builds hung to the 6-hour job limit *after* `npm publish` had already succeeded, so **`0.6.1` and `0.6.2` both reached npm with no GHCR image and no GitHub release**. arm64 now builds on native `ubuntu-24.04-arm` runners, each arch pushes by digest with a manifest-merge job, and every publish step is idempotent so a half-finished release is recoverable via `workflow_dispatch`. Both releases were recovered that way (GitHub releases for `v0.6.1`/`v0.6.2` are both dated 2026-07-28; all four GHCR tag sets exist). See `docs/RELEASING.md` §2 Step 6.
- **Coverage ratchet raised (2026-07-27)** — the filtering coverage recovery + dead-code removal (#182) lifted the gate from 80/78/89/89 to **83 branches / 82 functions / 92 lines / 92 statements** (`jest.config.js`), per the ratchet policy in CLAUDE.md. That is still the gate on `main`; honest coverage has since run well ahead of it (see the table below), so a raise is due.
- **Per-version e2e stacks (#205/#206, 2026-07-28)** — the e2e harness moved from one shared, version-re-pinned stack to **one persistent stack per `<version>-<db>` target** with derived ports (`2.4.0-postgres` → 8240, `2.3.0-postgres` → 8230) and stable, reused tokens; `npm run e2e:up` now selects its stack via `VIKUNJA_E2E_TARGET`, not `VIKUNJA_VERSION`. Concurrency-safe: one worktree's `e2e:down` no longer rotates another's credentials out from under it. (Two known rough edges remain: #219, `scripts/oidc-e2e.ts` still defaults to the pre-#206 port; #218, an empty `VIKUNJA_E2E_TARGET` bypasses the default.)
- **OIDC resource-server epic (waves H1+H2) — MERGED to `main` 2026-08-14 (#216), no longer a branch.** The multi-user HTTP/OIDC resource-server design (`docs/OIDC-RESOURCE-SERVER.md`) — stateless Streamable-HTTP transport, per-request JWT validation, per-identity isolation, the encrypted credential vault, self-service `vikunja_auth` provisioning, and a mock-OIDC e2e lane — landed on `main` after owner testing, closing the open merge-back decision decision 16/20 tracked. RFC 9728 protected-resource discovery came with it (#216); **one-click SSO enrollment** followed in #221 (`/enroll` + `/enroll/callback`, single-use identity-pinned tickets, server-side token minting into the vault). Everything is inert unless `VIKUNJA_MCP_TRANSPORT=http` — **the default `stdio` deployment is byte-for-byte the pre-epic single-tenant behavior.** Two enrollment identity-pinning defects found after the merge are open and honest: #223 (2.4.0's `GET /user` returns no email, so an email-first match can never succeed) and #224 (username squatting when the OpenID account gets a random username).
- **`0.7.0-beta.0` and `0.7.0-beta.1` (both 2026-08-14) — the public OIDC beta.** Published on npm's `beta` dist-tag and GHCR `:beta`; `latest` deliberately untouched at `0.6.2`. `beta.0` is the resource-server mode itself, `beta.1` the one-click SSO enrollment. The release pipeline was made prerelease-aware first (#214) so a `-beta.x` tag publishes to `beta`/prerelease and never steals `latest`. **`0.7.0` is therefore the OIDC release, not the v2 release** — see the v2 bullet below.
- **Silent-data-loss and reliability hardening batch (16 PRs, 2026-08-24 → 2026-08-31)** — the largest correctness pass since Wave B, all merged to `main` and unreleased at the time of writing (`CHANGELOG.md` "Unreleased"). Four themes: (1) **silently-dropped fields** — a whole class, 16 gaps closed across tasks, projects/views, teams, webhooks, batch-import and `setup-kanban` (#229, #236, #239, #240, #243), each gap now either supported or *loudly rejected* with a teaching error; (2) **full-replace endpoints** — team update was silently un-publishing public teams and `vikunja_users update-settings` was wiping name/language/timezone/week-start/default-project/discoverability/reminder preferences on every call; both are now read-then-merge (#230, #238), and `vikunja_webhooks update` now rejects `targetUrl`/`secret` because `Webhook.Update` is a hard-coded single-column write; (3) **retry and filter honesty** — creates are never retried after an ambiguous failure (#228, decision 23), date-only values are coerced to RFC3339 on the create paths that used to 400 (#242), and filtered listings no longer report wrong or partial results as success (#244: label filters resolve titles to ids, filter date literals are coerced, per-project aggregation follows every page instead of the server's clamped first 50, and an unhonourable filter is an error naming the unresolved labels rather than `Found 0 tasks`); (4) **central log redaction** (#241, decision 26). One **BREAKING** change rides along: `percentDone` is now an integer percentage 0-100 on the whole tool surface (#231, decision 22). Battle coverage grew 13 → 21 scenarios (#235) and three documentation-currency passes landed (#232/#233/#234).
- **v2 Vikunja API adoption — re-slotted from `0.7.0` to `0.8.0`, still not started.** Decision 20 named `0.7.0` as the v2 migration release; that is **superseded** — `0.7.0` became the OIDC release, and v2 is now tracked as the `0.8.0` milestone in issue [#184](https://github.com/netadvanced/vikunja-mcp-ng/issues/184) (its title says `0.8.0`; the issue body still reads "0.7.0 milestone" and is stale on that point). The groundwork from decision 17 stands: `0.6.0` vendored the v2 OpenAPI spec and generated types (#147, not wired into runtime) and the v2 PATCH route remains the intended replacement for the fetch-merge-POST/snapshot-restore workarounds decisions 14/15 document. Two live findings sit in front of that wave and are recorded rather than assumed away: v2 `PATCH` returns 422 on **subscribed** tasks (workaround: `subscription: null`), and Vikunja 2.6.0 makes *task creators* auto-subscribe — which widens that blocker from "assignees" to "every task we create" (#237). No date; scheduling is an owner call.
- **Vikunja 2.5.0 and 2.6.0 are released upstream and are NOT supported here.** The aligned/tested default stays **2.4.0**, and since 2026-08-31 so does the floor (decision 27 — raised from 2.3.0; the two now coincide until `aligned` moves); nothing in `src/` targets 2.5 or 2.6, the vendored spec is still 2.4.0, and no e2e target exists for either. This is a deliberate, documented position, not a claim of compatibility — issue [#237](https://github.com/netadvanced/vikunja-mcp-ng/issues/237) carries the alignment analysis with three concrete exposures (2.6.0's API-token scope check can start 403-ing `expand` passthrough for `tk_*` sessions, our default auth mode; task-position writes now verify the view belongs to the task's project, which the explicit-`projectViewId` path does not check; and the creator auto-subscribe risk to #184 above) plus the alignment-target decision itself (2.5.0, 2.6.0, or hold at 2.4.0 and raise only the floor policy). 2.6.0 is primarily a security release — 18 fixes, 5 high — so the exposures are tightened enforcement, not new features. **The operation count is a misleading measure of this gap:** a tag-to-tag spec diff finds zero operations added 2.4.0→2.5.0 and exactly one (`DELETE /notifications`) 2.5.0→2.6.0, yet a review of the 2.4.0→2.6.0 delta against what a client like ours actually does classifies it as roughly **17 breaking / 21 behaviour-changing / 14 additive** — almost all of it in handler enforcement the spec does not describe. Alignment is a behavioural re-verification job, not a spec refresh. **No timeline is set.**
- **Active community contributor stream** — three PRs from an external contributor (@angusmaul: #89 native bulk-update contract, #116 bulk-create SQLite-lock serialization, #122 bucket position param) have been reviewed and merged. *(Note: an earlier draft of this section assumed four merged external PRs; a fresh count against `gh pr list --state merged` on 2026-07-21 found three — corrected here.)*

**Live numbers as of this document** (measured fresh in a clean worktree, 2026-08-31):

| Metric | Value |
|---|---|
| Test suites | 181 passed / 181 total |
| Tests | 4,311 passed / 4,311 total, 0 failing |
| Coverage (stmts / branches / funcs / lines) | 93.70% / 85.65% / 84.56% / 93.80% vs. the ratcheted gate **92 / 83 / 82 / 92** — the gate as it stands on `main` (`jest.config.js`, last raised 2026-07-27 with #182). Honest coverage has run 1.6–2.6 points ahead of every threshold, so under the CLAUDE.md ratchet policy a raise is due; it may already be in flight in a parallel PR. |
| Registered top-level tools | **27** (re-verified 2026-08-31 via `grep -rhoE "'vikunja_[a-z_]+'" src/tools/ \| sort -u \| wc -l` — unchanged since 2026-08-03; OIDC mode adds no top-level tools), ~150 subcommands across them — 18 register for any authenticated session under the default config, 9 are JWT-only and/or deny-by-default. *(Earlier revisions said 24: that counted `register*Tool` **functions** in `src/tools/index.ts`, and `registerExportTool` alone registers four tools. Tool names are the honest unit — it is what an MCP client sees. See [TOOLS.md](TOOLS.md).)* |
| Battle scenarios | **21** in `scripts/battle/scenarios/` (was 13; +8 in #235 — teams, task position, three `percentDone` paths, bulk-update partial failure). Last full paid run: 13/13 on haiku at $0.4555 (2026-08-31, before the 8 new scenarios landed). |
| API operations covered | 123 ✅ implemented + 1 ⚠️ implemented-with-bug + 1 🟡 partial + 44 ❌ not implemented (36 PARKED + 8 NEVER, see §4) = 169 total (73% clean-implemented) — unchanged by the 2026-08 batches, which closed *field-level* gaps on already-implemented operations rather than adding operations. The denominator is against the vendored **2.4.0** spec — a 2.4.0-surface denominator, which since the 2026-08-31 floor raise (decision 27) is also the floor surface, so it no longer over-claims (see §4's 2026-08-31 note). 2.5.0/2.6.0 have not been vendored; an external tag-to-tag diff reports 2.4.0→2.5.0 adds nothing and 2.5.0→2.6.0 adds exactly one operation (`DELETE /notifications`) — the operation count is the *wrong* measure of that gap (#237). |
| Live verification | `npm run test:mcp` and the MCP-layer e2e harness pass on both DB backends at Vikunja 2.4.0, which is now both the aligned version and the floor — so the standard target set is one version × two backends and there is no separate floor lane (decision 27). The last green run at the old 2.3.0 floor is recorded in decision 18. See `docs/LOCAL-TESTING.md`. A mock-issuer OIDC lane (`npm run test:e2e:oidc`) covers the HTTP transport and the SSO-enrollment chain. |
| Version / npm dist-tags | `0.7.0-beta.1` (`package.json`); `npm view vikunja-mcp-ng dist-tags` → `latest: 0.6.2`, `beta: 0.7.0-beta.1` (verified 2026-08-31) |
| Releases shipped | `0.1.0` → `0.7.0-beta.1` (**14 version tags**, counted 2026-08-31 via `git tag`; the non-version `pre-history-removal` tag is not a release). `0.5.1` was the first published via the tag-triggered OIDC-Trusted-Publishing workflow; `0.7.0-beta.0`/`.1` were the first prereleases through the prerelease-aware pipeline (#214) — see `CHANGELOG.md` and `docs/RELEASING.md` |
| Vikunja compatibility | floor **2.4.0** (v1 API, raised from 2.3.0 on 2026-08-31 — decision 27), aligned/tested **2.4.0**; floor and aligned coincide until `aligned` moves. **2.5.0 and 2.6.0 released upstream and unsupported here** — see #237 |

**Known-limited today (deliberate, tracked):**
- `vikunja_templates` is session-only (in-memory) by default; opt-in JSON file persistence shipped in Wave F (PR #78) via the `templates.persistPath` config key / `VIKUNJA_MCP_TEMPLATES_FILE` env var (see §3a(b) — this is *why* SQLite was parked, not a competing effort; §3a(b) also records why SQLite was later removed outright, not just left parked).
- User-data export can only trigger/report the server-side export — MCP cannot deliver binary files.
- **General per-PR/branch GitHub Actions CI remains off by explicit, standing owner decision** (cost preference; all gates run locally per PR instead) — see §3b. This is narrower than it used to be: the tag-triggered release workflow (`.github/workflows/release.yml`, installed 2026-07-20) *is* live CI, it just only ever runs on a `v*` tag push, never on ordinary PRs/branches; re-verified 2026-08-31 — it is still the only workflow file in the repository. Don't conflate "release CI is live" with "PR CI is on" — both are true/false independently.
- **OIDC/HTTP mode is merged and in beta, not in a stable release.** It is only reachable via `VIKUNJA_MCP_TRANSPORT=http` and only on the `beta` dist-tag; `latest` (`0.6.2`) does not contain it. Enrollment identity-pinning has two open defects (#223, #224).
- **Vikunja 2.5.0/2.6.0 are unsupported** — see the bullet above and #237. We do not claim compatibility, and the tightened 2.6.0 enforcement has known exposures in our code.
- v2 Vikunja API adoption has not started (deliberately staged — now the `0.8.0` milestone, issue #184; see above and §6).
- The BREAKING `percentDone` 0-100 change (decision 22) and the whole 2026-08 hardening batch are on `main` but **unreleased** — no published version carries them yet.

## 3. Locked architecture decisions

This log is **append-only** — never edit or remove an existing row; add new dated rows as decisions are made or revised.

| # | Decision | Date |
|---|---|---|
| 1 | Single tracking issue (#28) + PR-per-change on this fork; no direct commits to main | 2026-07-17 |
| 2 | `node-vikunja` EOL: no new call sites ever; full per-domain migration; dependency removed at the end | 2026-07-17 |
| 3 | All new HTTP via `vikunjaRestRequest` + types generated from the vendored OpenAPI spec | 2026-07-17 |
| 4 | Composite-first tool design; spec = coverage checklist, not tool design | 2026-07-17 |
| 5 | Module enable/disable config: layered file+env, registration-time gating, config can only narrow auth, deny-by-default for dangerous modules | 2026-07-17 |
| 6 | Secrets via `*_FILE` env variants (Docker Swarm/K8s); never in the config file; both-set = startup error | 2026-07-17 |
| 7 | Saga/compensation helper for composites; opt-in `atomic`; destructive steps last; no fake ACID | 2026-07-17 |
| 8 | Upstream courtesy reports (democratize-technology/vikunja-mcp + node-vikunja) batched after our fixes land | 2026-07-17 |
| 9 | `node-vikunja` dependency fully removed from `package.json`; grep-zero proven; project repackaged as `vikunja-mcp-ng` with Docker distribution | 2026-07-18 |
| 10 | Go rewrite: **parked** (not rejected). See §3a(a) for the case and the reopening condition. | 2026-07-18 |
| 11 | SQLite persistence: **parked** in favor of an opt-in JSON file store for templates. See §3a(b) for the reopening trigger. | 2026-07-18 |
| 12 | Positioning adopted from the 2026-07-18 competitive review: global read-only mode + MCP tool annotations (adopted tonight, sibling PRs); version-pinned live e2e (adopted earlier, Wave D). See §3b. | 2026-07-18 |
| 13 | Endpoint-tail re-triage (C2): all 64 `❌` ops re-examined under direct REST. §4's won't-implement list rewritten to a 20 IMPLEMENT / 36 PARKED / 8 NEVER split (no coverage-count change); backlog G1–G7 in `docs/ENDPOINT-TAIL-RETRIAGE.md`. Supersedes the reasoning (not the append-only history) of decision 4's original won't-implement framing. | 2026-07-18 |
| 14 | The PR #63-era decision "bulk-update always per-task, never native" is **SUPERSEDED**. It was conditioned on `node-vikunja`'s malformed `{task_ids, field, value}` payload; that library is fully removed, and external PR #89 (credit: angusmaul) verified the real `models.BulkTask` contract `{task_ids, fields, values}` against a live Vikunja 2.3.0. New decision: native single-request bulk (`POST /tasks/bulk`) is now primary for scalar fields, with per-task fallback retained — **native-first because the direct-REST contract is verified and a single request avoids SQLite lock partial failures**. Condition to revisit: if the bulk endpoint's assignee-clearing bug is fixed upstream, the snapshot/restore workaround (PR #89) can then be dropped. Follow-up hardening (server-derived honest counts, surfaced assignee-restore failures, field allowlist) landed in the same PR wave. | 2026-07-18 |
| 15 | Follow-up to decision 14's revisit condition: source-verified 2026-07-18 against go-vikunja/vikunja source (`pkg/models/tasks.go`, `task_assignees.go`) — the bulk endpoint's assignee-clearing behavior is unfixed on `main` as of commit `2b7d31bfb`; no upstream issue found reporting this specific `fields`-vs-assignees gap (closed go-vikunja/vikunja#1459 is the closest prior art but predates the `fields`/`values` design added by #1456). A draft issue was prepared but not filed — pending owner review. Separately, the same verification pass found the bulk endpoint's `values.assignees` IS honored (just orthogonal to `fields`, unlike the "cannot be set at all" wording decision 14 implied) — this unlocked a narrow simplification: the post-bulk-update assignee-*restore* step (a complete per-task snapshot, not a general assign) now issues ONE `POST /tasks/{taskID}/assignees/bulk` (`models.BulkAssignees`) call per task instead of a per-user `PUT /assignees` loop, same `assigneeRestoreFailures` reporting contract. The snapshot/restore workaround itself (decision 14) stays — its revisit condition (upstream fix) remains open. | 2026-07-18 |
| 16 | **0.6.0 OIDC resource-server epic approved.** Full design in [docs/OIDC-RESOURCE-SERVER.md](OIDC-RESOURCE-SERVER.md) (owner review locked 2026-07-20). Four owner decisions anchor the doc: (a) credential vault = an **encrypted JSON file** (AES-256-GCM via `node:crypto`, per-record IV, master key via the existing `_FILE` secrets machinery, atomic write-then-rename mirroring `src/storage/templateFileStore.ts`) — **not** `better-sqlite3`; (b) `better-sqlite3` is **dropped from `package.json` entirely** (see below — this closes the loop on decision 11/§3a(b)); (c) circuit breakers **stay shared** across users (they protect the one shared upstream Vikunja; per-user rate limits handle noisy neighbors independently; accepted, honestly-documented cross-user coupling, revisit only if multi-Vikunja-instance support lands); (d) all other open questions the design raised are accepted as-is — stateless `StreamableHTTP` transport, `AsyncLocalStorage` request context, self-service provisioning via new `vikunja_auth` `provision`/`status`/`deprovision` subcommands (`sub` always from the validated token, never an argument), per-sub rate limits plus an optional global ceiling, a mock OIDC issuer as the e2e default with an optional real-Keycloak lane, `jose` promoted to a direct pinned dependency, and single-issuer config first with `(issuer,sub)` internal keying so multi-issuer stays additive. Two-wave plan (H1 transport+JWT+identity/isolation with a stub credential source; H2 vault+provisioning+hardening+mock-issuer e2e) with acceptance criteria is in the design doc. **`better-sqlite3` removal**: this closes the loop on decision 11 (§3a(b), SQLite parked 2026-07-18) — its reopening trigger ("a real relational or query need... simple durability alone does not meet this bar") was checked against the OIDC vault's needs and did **not** fire; the vault is a single key→token lookup, not a relational workload, so `better-sqlite3` (proven zero-usage by grep across `src/`, `tests/`, `scripts/`) is removed from `package.json` and the lockfile rather than finally being put to use. | 2026-07-20 |
| 17 | Follow-up to decision 14's revisit condition: the maintainer closed go-vikunja/vikunja#3222 ("`POST /tasks/bulk` (and `POST /tasks/{id}`) always overwrite assignees even when `fields` doesn't list them") as **NOT_PLANNED** (confirmed via `gh issue view 3222 --repo go-vikunja/vikunja`). Maintainer's exact words (`gh issue view 3222 --repo go-vikunja/vikunja --json comments`): "As you pointed out, this is due to a limitation in the way Go handles nil values. Because of that, what you're describing is actually expected behaviour, not a real bug. The new v2 api has a patch route that can be used when assignees should be preserved." I.e. this is by-design v1 nil-semantics, not a bug they intend to fix — there is no upstream fix to wait for. Decision 14's snapshot/restore workaround therefore **stays in place as-is**; its revisit condition changes from "wait for an upstream v1 fix" (now confirmed will never land) to: migrate `bulk-update` (and similar fetch-merge-POST workarounds, e.g. the view-update and bucket-update composites noted in `docs/API-COVERAGE.md`) to the v2 API's PATCH task route, once v2 is available/stable enough on our pinned Vikunja server version. A companion investigation (same session, delivered as a separate report, not part of this PR) analyzed exactly that v2-adoption question — a GO/WAIT recommendation plus a candidate migration list — tracked as the follow-up for this condition. | 2026-07-20 |
| 18 | **Aligned to Vikunja 2.4.0** (tracking issue #28, item A1). The e2e stack's default pin moved `2.3.0` → `2.4.0` (`docker/e2e/docker-compose.yml`, `docker/e2e/bootstrap.sh`); the vendored OpenAPI spec (`docs/vikunja-openapi.json`) was refreshed by fetching directly from the pinned 2.4.0 container's own `/api/v1/docs.json` (a new `fetch:api-spec:container` npm script, now the documented default over `try.vikunja.io`'s always-ahead-of-tag `unstable` spec — see `docs/API-SPEC.md`); `npm run generate:api-types` regenerated with zero typecheck fallout (the only spec diff: 5 documented response codes corrected `200`→`201` for creation endpoints, plus one new, irrelevant-to-us error-schema field `web.HTTPError.i18n_params`). The `scripts/mcp-e2e.ts` assignees-500 `driftTolerated()` special-case (upstream go-vikunja/vikunja PR #2791) is now **version-gated** (tolerated only when the detected live server is `< 2.4.0`) rather than global, since the fix is confirmed shipped in the 2.4.0 tag — a same-signature 500 on 2.4.0+ now hard-fails as a real regression. **Explicit policy: minimum supported Vikunja is 2.3.0 (v1-floor); aligned/tested default is 2.4.0.** Proof matrix (`npm run test:matrix`, both harnesses, `e2e-verdicts/`): `2.4.0`/postgres PASS (zero tolerated drift beyond the pre-existing, unrelated JWT-only user-webhook-scope tolerance), `2.4.0`/sqlite PASS (same), `2.3.0`/postgres PASS as the v1-floor regression run (assignees-500 correctly still tolerated there, per the version gate). The 12-task `bulk-create` stress check (#116) was re-run 5× against `2.4.0`/sqlite: 12/12 clean every time, consistent with the new `concurrent_writes: true` field Vikunja 2.4.0's `GET /api/v1/info` now advertises — see `docs/LOCAL-TESTING.md`'s "Vikunja 2.4.0 and `concurrent_writes`" section. Client-side write-serialization is **retained regardless as defense-in-depth** (the v1-floor, 2.3.0, lacks the fix). The v1-floor-only workarounds (by-id share GET via list, decision 14/15/17's assignee snapshot/restore, and the sequential-writes serialization above) **all stay** — none is conditioned on the *default* pin, only on raising the *minimum supported* version, which this item deliberately did not do. v2 API adoption (decision 17's follow-up condition) remains explicitly **out of scope** for this item — alignment only. | 2026-07-20 |
| 19 | **2.4.0 coverage re-audit (item T2) — denominator honest, no new surface.** Follow-up to decision 18 (which refreshed the vendored spec to `v2.4.0`): before cutting 0.6.0, the operation set was diffed method-by-method rather than trusted from prose. Every `(method, path)` in the vendored `v2.4.0` spec (169) was enumerated and diffed **both ways** against the prior `v2.3.0-1019-g95b7e673` spec `docs/API-COVERAGE.md` was denominated against (`git show 427fb0f^:docs/vikunja-openapi.json`): **0 new ops, 0 removed ops** — operation set identical, **denominator stays 169**, header accounting unchanged (123 ✅ / 1 ⚠️ / 1 🟡 / 44 ❌). Schema (`definitions`) diff: 98 models both sides, 0 added/removed; only field-level change is `web.HTTPError.i18n_params` (error envelope, no row exposes it); the 5 `200`→`201` response-code corrections hit already-implemented creation endpoints and change no status. Big-model spot-check (Task/Project/Bucket/TaskComment/Label/ProjectView) — identical property sets, so **no silently-unexposed new-in-2.4.0 fields**. **No `IMPLEMENT` verdicts emerged** (no new operations to triage); the approved-to-build backlog is unchanged from decision 13's C2 re-triage. Net: the 2.4.0 alignment adds **zero new API surface to chase** — the remaining 44 `❌` (36 PARKED + 8 NEVER) is the complete picture for the 0.6.0 decision. Live stack was down during the pass; vendored spec used as ground truth (A1's proof matrix already validated it against the running 2.4.0 container). | 2026-07-21 |
| 20 | **`0.6.0` shipped (2026-07-24) without the OIDC epic; OIDC merge-back re-slotted to `0.7.0`-or-later; `0.7.0` confirmed as the v2 API migration release.** `0.6.0` — the *"optimised for Vikunja 2.4"* milestone decision 18/19 built toward — released as a reliability + agent-ergonomics batch (circuit-breaker cascade fix, silent date data-loss fix, Kanban/filter ergonomics, label attach-by-title, per-session v2/capability detection) and, separately, is the first release to publish genuinely multi-arch GHCR images (`linux/amd64` + `linux/arm64`, SLSA provenance, #146); verified current via `npm view vikunja-mcp-ng dist-tags` → `latest: 0.6.0` and `git tag -l -n99 v0.6.0`. Two things this release confirms that earlier docs only implied: (1) the OIDC resource-server epic (decision 16, waves H1+H2, feature-complete on `feat/oidc-mode` since 2026-07-21) was **not** merged as part of `0.6.0` — RELEASING.md §3's owner-discretion note and this roadmap's §6 Horizon previously described the OIDC merge-back as tied to "the accompanying `0.6.0` release"; that framing is now superseded, and the merge-back is an open decision for **`0.7.0` or a later minor**, still pending owner testing, no date set. (2) `0.6.0` only vendored the v2 OpenAPI spec/types as prep (#147, not wired into runtime) — the v2 API fast-path migration itself did not ship in `0.6.0`; `0.6.0`'s own CHANGELOG "Internal" section names the target directly: *"preparation for the v2 API migration (0.7.0)"*. Both RELEASING.md §3 and this roadmap's §2/§6 are updated to reflect this rather than continuing to describe `0.6.0` as upcoming. | 2026-07-24 |
| 21 | **`0.6.1` scoped as an ergonomics/bugfix release, led by issue [#173](https://github.com/netadvanced/vikunja-mcp-ng/issues/173).** Found via post-`0.6.0` battle-campaign transcript analysis (2026-07-23/24): the `q3-offsite-kanban` scenario's underlying bugs are gone (0 breaker hits after `0.6.0`), but the flow is still call-heavy for a weaker agent — haiku pass rate 2/3, ~38 tool calls vs. an optimal ~15, pure multi-step orchestration overhead, not a correctness bug. Proposed fix: a `setup-kanban` composite (project + view/buckets + bulk-create + placement in one call). Framed by the owner explicitly as a `0.6.1` bug/ergonomics release, not part of the `0.7.0` v2-migration or OIDC-merge-back scope. Not started as of this row. | 2026-07-24 |
| 22 | **`percentDone` is a whole percentage 0-100 on the tool surface, integers only** (BREAKING, `0.7.0-beta`). Vikunja's wire contract for `models.Task.percent_done` is a fraction 0-1 (`PercentDone float64`, `pkg/models/tasks.go`; the frontend picker stores `[0, 0.1, … 1]` in `PercentDoneSelect.vue`) and that has not changed — what changed is where the conversion happens. It now happens once, at this server's boundary (`src/utils/percent-done.ts`), on every path the field crosses: `create`, `update`, `bulk-create`, `bulk-update`'s raw `percent_done`, `vikunja_batch_import`, the read/display path, and `percentDone` inside a filter string (including saved filters, which are stored server-side in Vikunja's own scale and converted back on read so `get` → edit → `update` round-trips). **Conclusion: 0-100 because the wire fraction leaked an implementation detail agents had to memorize — revisit if Vikunja changes its own human-facing scale.** The supporting evidence: a real Claude session using this MCP recorded the 0-1 scale in its own list of "gotchas" (a model compensating for the interface — that memory dies with the session and transfers to no other MCP client or weaker model); Vikunja's own i18n describes the field as 0-100, so only the wire is a fraction; two independent upstream contributors (democratize-technology/vikunja-mcp#94, #82) also assumed 0-100; and design pillar 1 (§1) says the OpenAPI spec is our *coverage checklist*, not our tool design. **Integers only is a safety property, not a style choice**: under 0-1 an agent passing `percentDone: 1` meaning "done" silently wrote 1% — accepted, no error, wrong data. Requiring an integer makes `1` unambiguously 1% and makes `0.5` a loud validation error that teaches instead of a silent 0.5%. Timing was itself part of the decision: cheap on `0.7.0-beta`, expensive after issue #183 declares the tool surface stable at 1.0. | 2026-08-31 |
| 23 | **A create is never retried after an ambiguous failure.** Vikunja's v1 API uses `PUT` as its CREATE verb (and `POST` as update), so `vikunjaRestRequest` now gives every `PUT` a retry predicate — `shouldRetryNonIdempotentWrite`, `src/utils/vikunja-rest.ts` — that resends **only** on HTTP 429 and on the pre-request network codes that prove no request bytes ever reached the application (`ECONNREFUSED`, `ENOTFOUND`, `ENETUNREACH`, `EAI_AGAIN`, `UND_ERR_CONNECT_TIMEOUT`). Everything else is **ambiguous**: a 5xx, a mid-flight `ECONNRESET`/`ETIMEDOUT`/`EPIPE`, an undici header/body timeout, or an open circuit breaker are all indistinguishable from "the server committed the write and the response was lost on the way back", so a resend silently creates a duplicate. Idempotent methods keep the previous policy unchanged, and a call site can override per request via `options.retry.shouldRetry`. The decision deliberately lives at the choke point rather than at each create call site, so a new create cannot forget it. **Conclusion: never retry an ambiguous create, because a silent duplicate is worse than a surfaced failure the caller can re-decide — revisit if Vikunja ever supports an idempotency key** (an `Idempotency-Key` header or equivalent removes the ambiguity, at which point creates can safely retry 5xx again). Landed in #228 (merged 2026-08-24); recorded here 2026-08-31. | 2026-08-31 |
| 24 | **Nested array-of-object schemas are strict; top-level tool schemas are deliberately not.** Every nested item shape an agent can pass — `setup-kanban`'s `tasks[]`, `vikunja_tasks`' `tasks[]`/`subtasks[]`, `vikunja_task_bulk`'s `tasks[]` — is validated through a `strictNestedObject` helper (`src/utils/strict-nested-object.ts`) that rejects an unknown key, names it, and lists the supported fields. Top-level tool argument shapes stay non-strict. **This is not an inconsistency to be tidied up later; it is the decision.** A nested item has a small, closed vocabulary owned by exactly one subcommand, so an unrecognised key there is always a mistake — historically a *silent* one, where the agent's field was dropped and the call still reported success. A top-level shape is shared across every subcommand of its tool and legitimately carries aliases and neighbours (`id`/`projectId` and friends) that are meaningful for one subcommand and inert for another; making it strict would reject working calls. **Conclusion: strict where the vocabulary is closed and single-owner, permissive where it is shared — revisit if top-level shapes stop being shared, e.g. if each subcommand ever gets its own discriminated schema, at which point they can be tightened on the same reasoning.** Landed in #236 (with the related field-gap closures in #229/#239/#240/#243). | 2026-08-31 |
| 25 | **A Vikunja full-replace endpoint gets read-then-merge at this server's boundary — now the established pattern across projects, teams and user settings.** Vikunja v1 has no partial update: several handlers bind the request body into an empty struct and write it wholesale, so a partial payload does not "leave the rest alone", it destroys it. The tells are specific and worth memorising: an `xorm` `UseBool("col")` (writes the column even when false — `Team.Update` silently un-published any public team on a description-only edit, #230), an explicit `Cols(...)` list (persists zero values — the project-view update handler would reset a view's position and blank its filter), and `forceOverride: true` (`POST /user/settings/general` was wiping name, language, timezone, week start, default project, both discoverability flags and reminder preferences on **every** call, #238). Where the endpoint *is* a full replace, we fetch current state and merge the caller's fields over it. Where it is neither full-replace nor partial — `Webhook.Update` is `s.Where(...).Cols("events").Update(w)`, a hard-coded single-column write — the honest answer is not to merge but to **reject** the unchangeable fields loudly (`targetUrl`/`secret` on `vikunja_webhooks update`, #238) rather than accept them and report a success that did nothing. **Conclusion: read-then-merge because v1 cannot express a partial update and a partial body therefore silently destroys data — revisit per call site as the v2 PATCH route is adopted (issue #184), which removes the need everywhere it reaches; the reject-loudly variant stays regardless, since it reflects the handler's shape, not v1's nil semantics.** | 2026-08-31 |
| 26 | **Log redaction is central, in the logger, not per call site.** A `sanitizeForLogging` pass now runs inside `Logger.log` (`src/utils/logger.ts`) over every argument, plus a `redactSecretsInText` backstop over the fully rendered line to catch credentials interpolated into a message literal where no key name exists to match on. Redaction covers what key-name matching cannot see: secrets in URL paths, URL userinfo, sensitive query values, and tokens embedded in prose. It runs **after** the level gate, so it costs nothing when the level is suppressed. **Conclusion: central because per-call-site discipline had already been tried and had already failed silently** — `sanitizeLogData` existed in `src/utils/security.ts` and was simply never wired into the logger, which is exactly the failure mode a per-call-site rule produces: nothing is visibly broken, so nobody notices. **Revisit only if a call site legitimately needs unredacted output — which would be an argument for an explicit, named opt-out at that site, not for decentralising the default again.** Two notes for whoever touches this: wiring the pre-existing `sanitizeLogData` verbatim would have broken logging (it renders an `Error` as `{}` and any string over 1000 chars as `[SANITIZATION_FAILED]`), which is why a separate log-shaped sanitizer was written; and this is operator-visible — some fields, notably `user`, now render `[REDACTED]`. Landed in #241. | 2026-08-31 |
| 27 | **Minimum supported Vikunja raised 2.3.0 → 2.4.0; aligned/tested stays 2.4.0, so floor and aligned currently coincide.** The 2.3.0 floor was never true in practice: **nine operations this project ships as `✅ Implemented` do not exist on a released Vikunja 2.3.0** — the eight `vikunja_admin` operations (`overview`, `list-projects`, `set-project-owner`, `list-users`, `create-user`, `delete-user`, `set-user-admin`, `set-user-status`) and `GET /projects/{project}/tasks/by-index/{index}` (`vikunja_tasks get-by-index`). They looked in-range because `docs/API-COVERAGE.md`'s 169-operation denominator was measured against `v2.3.0-1019-g95b7e673`, a `try.vikunja.io` **unstable build 1019 commits past the tag**; the released 2.3.0 has 160. **Conclusion: raise the floor, because that makes the compatibility claim true, whereas documenting a caveat would only make a false claim better-annotated — and secondarily because upstream moves fast and this project needs to keep up. Revisit when the aligned version moves (issue #237 carries the 2.5.0/2.6.0 analysis): floor and aligned coinciding is an acceptable transient, not a target state, and the pre-tag floor-regression lane in `docs/RELEASING.md` — deliberately pointed at a version different from the default so it never gets exercised by accident — only regains its meaning once they separate again.** Per `docs/RELEASING.md` §1 this is at least a minor bump; it rides the existing `0.7.0` beta line at no extra cost. Mechanically: `MIN_SUPPORTED_VIKUNJA` (`scripts/lib/vikunja-compat-version.sh`) and `FLOOR_VERSION` (`scripts/lib/e2e-target.ts`) are the two machine-readable sources of truth and both moved; `release.yml` already omits the duplicate `-vikunja<min>` image tag when `MIN == COMPAT`, so a coinciding floor publishes one compat alias instead of two identical ones; the 2.3.0 e2e target machinery is **kept** (port derivation is arithmetic and costs nothing) but 2.3.0 is no longer a standard target. Two workarounds whose stated removal condition was "floor ≥ 2.4.0" now have it satisfied — `findShareByIdViaList` (fully fired) and the bulk-create write-serialization (conjunction, only the first arm fired) — and are deliberately left in place: this decision is a policy change, not a behaviour change. | 2026-08-31 |


*(Editorial note, 2026-07-21: rows 18 and 19 were physically out of order above — row 19 was appended above row 18 despite being logically dependent on it and dated a day later. Reordered here to match append/chronological order; no row's content was edited, consistent with the append-only rule.)*

### Amendments to rows above (dated notes; rows themselves unedited, per the append-only rule)

- **Row 11 (SQLite parked, 2026-07-18) — CLOSED, 2026-07-20.** Decision 16 removed `better-sqlite3` from `package.json` entirely rather than reopening it: the OIDC vault's need was checked against row 11's reopening trigger ("a real relational or query need") and that trigger did **not** fire — the vault is a single key→token lookup. See decision 16 and §3a(b) below for detail.
- **Row 14 (bulk-update "SUPERSEDED", 2026-07-18) — revisit condition resolved, 2026-07-20.** Decisions 15 and 17 are the follow-ups: 17 confirms upstream closed the underlying issue as **NOT_PLANNED** (by-design v1 nil-semantics, not a bug), so row 14's original "wait for an upstream fix" condition will never fire. Its replacement condition — migrate to the v2 API's PATCH task route once v2 is stable enough on our pinned server — is tracked as a staged, not-yet-started horizon item in §6, now dated to `0.7.0` (decision 20).
- **Row 16 (OIDC epic approved, 2026-07-20) — implementation status, 2026-07-21; re-slotted, 2026-07-24; still open, 2026-08-03.** Waves H1+H2 are feature-complete on branch `feat/oidc-mode`, pending owner testing and a merge-back decision to `main`. As of decision 20, `0.6.0` shipped without this epic, so the merge-back now targets `0.7.0` or later rather than "the next release." Re-checked 2026-08-03: `feat/oidc-mode` still exists and is still not an ancestor of `main`; two further releases (`0.6.1`, `0.6.2`) shipped without it. See §2 and §6, and `docs/OIDC-RESOURCE-SERVER.md` for the up-to-date wave status.
- **Row 21 (`0.6.1` scoped, 2026-07-24) — DELIVERED, 2026-07-25.** The `setup-kanban` composite shipped in `0.6.1` (#173 → #175) along with the multi-task label subcommands (#178) and three fixes found by running the new composite against a live server (#176, #177) and in the changelog tooling (#174); the scenario's measured haiku pass-rate went 2/3 → 3/3 at one call instead of ~38. A follow-on correctness release, **`0.6.2` (2026-07-28)**, then closed the JWT-only coverage hole (#198) and the multipart-through-JSON-helper bug it exposed (#199), fixed the stale `initialize` version (#186) and the never-cleaned `dist/` (#187), and made `setup-kanban`'s `columns` optional (#185). Both shipped as **patches** by the §3 owner-discretion latitude recorded in `docs/RELEASING.md`, with `0.7.0` still reserved for the v2 migration. Neither release started v2 or merged OIDC, so decision 20's slotting is unchanged.
- **Row 20 (`0.7.0` = v2 API migration, 2026-07-24) — start trigger elapsed, work not started, 2026-08-03.** §6's Horizon set the trigger at "roughly two weeks of additional upstream v2 field time from 2026-07-20, **or** Vikunja `2.4.1`, whichever comes first." The calendar arm has now elapsed (no `2.4.1` observed; the e2e default pin and vendored spec are still `2.4.0`). Nothing has been re-decided here — recording only that the wait this row described is over, so `0.7.0`'s start is an owner scheduling call.
- **Row 16 (OIDC epic approved, 2026-07-20) — MERGED, 2026-08-14.** The merge-back decision this row's earlier amendments left open is closed: `feat/oidc-mode` landed on `main` via #216 (with RFC 9728 protected-resource discovery), followed by one-click SSO enrollment in #221, and shipped as `0.7.0-beta.0`/`0.7.0-beta.1` on npm's `beta` dist-tag. The four owner decisions anchoring the design (encrypted-JSON vault, no `better-sqlite3`, shared circuit breakers, stateless HTTP transport) shipped as written; `docs/OIDC-RESOURCE-SERVER.md` records where the shipped code diverged from the locked design, and `docs/OIDC-SETUP.md` is the install manual. Open follow-ups, recorded rather than closed: #223 and #224 (enrollment identity-pinning), #219 and #218 (e2e target/port defaults).
- **Row 20 (`0.7.0` = v2 API migration, 2026-07-24) — SUPERSEDED on the version number, 2026-08-31.** The row's substantive content stands: v2's PATCH route is still the intended replacement for decisions 14/15's fetch-merge-POST and snapshot/restore workarounds, and `0.6.0`'s vendored v2 spec/types (#147) are still the groundwork. What is superseded is the **slot**: `0.7.0` became the OIDC resource-server release (see the row 16 amendment above), and v2 adoption is now the **`0.8.0`** milestone, tracked in issue [#184](https://github.com/netadvanced/vikunja-mcp-ng/issues/184). Note for a future reader: #184's *title* says `0.8.0` but its *body* still opens "Tracking issue for the **0.7.0 milestone**" — the title is the current truth. The earlier amendment above (start trigger elapsed, 2026-08-03) is unaffected; there is still no upstream wait, only an owner scheduling call, and #237 has since added a reason to re-verify before starting (2.6.0's creator auto-subscribe widens the v2 PATCH-on-subscribed-task 422 from "assignees" to "every task we create").
- **Row 19 (2.4.0 coverage re-audit, 2026-07-21) — CLARIFIED, 2026-08-31; the diff is right, the label on it was misleading.** A fresh tag-to-tag comparison of the upstream `v2.3.0` and `v2.4.0` OpenAPI specs reports **160 → 169 operations, nine added** (`GET`/`POST /admin/users`, `DELETE /admin/users/{id}`, `PATCH /admin/users/{id}/admin`, `PATCH /admin/users/{id}/status`, `GET /admin/overview`, `GET /admin/projects`, `PATCH /admin/projects/{id}/owner`, and `GET /projects/{project}/tasks/by-index/{index}`), which appears to contradict this row's "zero new/removed operations". It does not. Re-run in this worktree: the row's "2.3.0-era" side is `git show 427fb0f^:docs/vikunja-openapi.json`, whose `info.version` is **`v2.3.0-1019-g95b7e673`** — a `try.vikunja.io` *unstable* build **1019 commits past the `v2.3.0` tag** — and it already contains all nine of those operations (169 ops on both sides, diffed both ways: 0 added, 0 removed). So both diffs are correct about different pairs, and the row's conclusion (denominator unchanged by the 2.4.0 refresh) holds. **What the row's wording obscured, and is worth stating plainly so it is not re-derived: the 169 denominator has never been the released-`2.3.0` surface** — that is 160 operations — and nine rows counted `✅` in §4 (the eight `/admin/*` operations behind `vikunja_admin`, plus `vikunja_tasks get-by-index`) target operations a **released Vikunja 2.3.0 server does not have**, even though 2.3.0 is our declared floor. Not a coverage-count correction; a floor-policy exposure, flagged in §4 and not yet triaged.
- **Row 18 (2.4.0 alignment, 2026-07-20) — its floor half is SUPERSEDED, 2026-08-31; its aligned half stands.** Row 18 recorded an *explicit policy*: "minimum supported Vikunja is 2.3.0 (v1-floor); aligned/tested default is 2.4.0", and deliberately did **not** raise the minimum — it says so twice, and every v1-floor-only workaround it lists was kept on exactly that basis. **Decision 27 raises the floor to 2.4.0.** The reason is not that row 18 reasoned badly but that a fact underneath it was wrong: the operation surface everyone was reasoning about was a `v2.3.0-1019-g95b7e673` unstable build, not the `v2.3.0` release, and nine operations this project already shipped do not exist on the release (see the row 19 amendment above and §4). Row 18's *aligned* half — the 2.4.0 pin, the container-fetched vendored spec, the regenerated types, the proof matrix — is untouched and still current. What is superseded is only the sentence "minimum supported Vikunja is 2.3.0" and the derived claim that a workaround stays because the floor lacks the fix: those workarounds now stay for narrower, separately-stated reasons (a pending live re-verification for the share GET; an unmet second arm of a conjunction for the write serialization). Row 18's recorded 2.3.0/postgres floor-matrix PASS also stands as history — it is simply the last such run, since the floor lane no longer exists while floor and aligned coincide.
- **Row 22 (`percentDone` 0-100, 2026-08-31) — what it supersedes, and why the old reasoning is kept.** The reasoning row 22 replaces is *"`percentDone` is a fraction 0-1 on the tool surface because that is Vikunja's wire contract"* — recorded in the `Added` entry #229 contributed to `CHANGELOG.md`'s unreleased cycle and in `docs/VIKUNJA_API_ISSUES.md`'s field-mapping section. It is superseded, **not** wrong about the wire: `models.Task.percent_done` is still a `float64` fraction and this server still sends one. What changed is the inference drawn from that fact — matching the wire was treated as a reason to expose the fraction, and §1 pillar 1 says the OpenAPI spec is our *coverage checklist, not our tool design*. Both places keep the superseded wording visible and marked rather than deleted, so a future reader can see that the fraction was considered and rejected as a tool-surface choice instead of re-deriving it from the spec.

### §3a. Evaluated options (parked)

Both items below were seriously considered on 2026-07-18 and explicitly **parked, not rejected** — each has a stated condition that would reopen it.

**(a) Go rewrite — parked 2026-07-18**

*The case for:* a single static binary and a tiny distribution image (no Node runtime, no `node_modules` layer); `go-vikunja`'s own generated model structs would give us the same "types from the source of truth" property we currently get from our generated OpenAPI types, without maintaining the generation step ourselves; the MCP-contract e2e harness built in Wave D (PR #67, spawns the real stdio server via the SDK client and asserts on the wire protocol, not the implementation) is a language-agnostic safety net — it doesn't care what language answers the stdio pipe, so a Go implementation could be validated against the exact same contract tests we already have.

*The case against:* the official MCP TypeScript SDK is the reference implementation the protocol spec itself is written against — a Go port inherits schema-validation and transport edge cases the TS SDK has already had shaken out. Our 3,258 verified tests (146 suites as of 2026-07-21, built up over many waves against real audited API bugs) don't port for free — they encode hard-won knowledge (fetch-merge-not-replace on task/project updates, the assignee bulk-payload shape, id-vs-username conflation on team routes, dozens more) that would all need re-deriving and re-testing in a second language. The opportunity cost of a rewrite (weeks, minimum) directly competes with the capability-coverage and hardening work still on the roadmap (§4's 44 remaining unimplemented operations).

*Reopening condition:* a bounded spike — 2–3 tools reimplemented on the official Go SDK, validated end-to-end by the existing MCP-layer e2e harness (no new test infrastructure, reuse what we have) — **if distribution pressure grows** (e.g., users routinely blocked by "no Node/Docker available" rather than by missing capability). Not reopened speculatively.

**(b) SQLite persistence — parked 2026-07-18**

*The case for:* `better-sqlite3` is already a dependency (flagged as possibly-unused in the 2026-07-18 competitive review — worth resolving either way); a real embedded database would give `vikunja_templates` durability across restarts and open the door to a local task cache or offline read-through.

*The case against:* the immediate, concrete need — durable templates — is served by a much smaller opt-in JSON file store, landing as a sibling PR in this same wave. That's a few dozen lines of read/write-with-fsync against a config-supplied path, no schema migrations, no query planner, no new failure mode (corrupt DB file vs. corrupt JSON file are comparably bad but the JSON case is trivially hand-editable/recoverable). Introducing SQLite for a single flat key-value need is disproportionate.

*Reopening trigger:* a **real relational or query need** — e.g., a local task cache with filtering/joins, or genuine offline support requiring more than "read the last-known JSON blob." Simple durability alone does not meet this bar.

**Update, 2026-07-20 (decision 16): closed, not reopened.** The OIDC resource-server design (`docs/OIDC-RESOURCE-SERVER.md`) needed a credential vault — a per-`(issuer, sub)` key→token lookup — and that need was checked against the reopening trigger above. It did **not** meet the bar (a single key-value lookup is not "a real relational or query need"). Rather than leaving `better-sqlite3` parked indefinitely as a zero-usage dependency, decision 16 removed it from `package.json` entirely; the vault is an encrypted JSON file (`src/storage/vaultFileStore.ts` on branch `feat/oidc-mode`), following the same pattern as the templates file store above. This section's reopening trigger stands unchanged for any *future* real relational need — it just wasn't this one.

### §3b. Positioning & adopted recommendations

A competitive review run 2026-07-18 compared this project against `@eargollo/vikunja-mcp`, a minimal single-dependency competitor. Bottom line: **our lane is comprehensive and governed** — composite/subcommand tool design that reduces round-trips for real task-management workflows, breadth of capability (admin, batch-import, export, templates, reactions, reminders — none of which the competitor has), spec-generated types, and layered module/auth gating. Theirs is minimal and defensively postured — one runtime dependency, a flat 1:1-endpoint tool surface, and a stricter default (nothing can write or delete without an explicit env-var opt-in). Both are legitimate, different positions; the review found no reason to change this project's roadmap direction, but it did surface concrete, adoptable ideas:

- **Adopted tonight (sibling PRs, this wave):** a global read-only / write-off-by-default mode (their `VIKUNJA_MCP_ALLOW_WRITE`/`VIKUNJA_MCP_ALLOW_DELETE` pattern, adapted to our module-gating shape as a coarse blanket toggle layered on top of it, not a replacement for it) and MCP tool annotations (`readOnlyHint`/`destructiveHint`/`idempotentHint`) so capable MCP hosts can auto-approve reads and gate confirmations on destructive calls without server-side prompting logic.
- **Adopted earlier (Wave D, PR #65):** a version-pinned live e2e harness (Docker Vikunja 2.3.0, deliberately not `:latest`) — the same shape the competitor uses for the identical reason: catching real API drift instead of trusting a client library's types.
- **Acknowledged gap, partially closed 2026-07-20, not fully:** at the time of the review, GitHub Actions was disabled repo-wide by explicit owner decision. That framing is now **superseded** — `.github/workflows/release.yml` (installed 2026-07-20) is a live, tag-triggered Actions workflow that re-runs the full gate suite plus publish steps on every release. What's *still* an acknowledged gap: **general per-PR/branch CI remains off** by a separate, still-standing owner decision (cost preference) — this project has no CodeQL, SHA-pinned workflows, TruffleHog secret scanning, or dependency-review gate running on ordinary PRs the way the competitor does; only a release tag triggers Actions here. Tracked, not disputed — revisit alongside the coverage ratchet (§2, §6) if per-PR CI is ever turned on.
- **Deliberately not adopted:** the flat 69-tool 1:1-REST-mirror shape itself — our composite/subcommand design is the more defensible choice for real usage and lower AI tool-list context cost at our target scale.

Correctness specifics found in the competitor's code during the review are not catalogued here, and no separate write-up of them is kept — this is a public repo with a solo maintainer, and the standard we hold ourselves to (§3, decision 8: batch courtesy reports, don't publish upstream bug catalogs unprompted) applies to other projects' bugs too.

## 4. Coverage: implemented / planned / won't-implement

Vikunja documents **169 API operations**. Fully re-verified row-by-row on 2026-07-18 against current `main` (not just a marker recount of the existing table — every row's status was checked against `src/tools/**`/`src/utils/vikunja-rest.ts`, see `docs/API-COVERAGE.md`'s "Updated as of" line). **The 169 denominator was re-confirmed against the vendored `v2.4.0` spec on 2026-07-21 (item T2, decision 19): a method-level operation-set diff found 0 new / 0 removed ops vs the 2.3.0-era spec, so this count does not change with the 2.4.0 alignment.**

| Status | Count | % |
|---|---|---|
| ✅ Implemented | 123 | 73% |
| ⚠️ Implemented (bug) | 1 | 1% |
| 🟡 Partial | 1 | 1% |
| ❌ Not implemented | 44 | 26% |
| **Total** | **169** | 100% |

**Two notes on this denominator, added 2026-08-31.** (1) **It is a 2.4.0-surface denominator, not a 2.3.0-floor one — and the floor was raised to 2.4.0 the same day, which is what closed the gap.** Decision 19's diff is sound, but its "2.3.0-era" side was the vendored `v2.3.0-1019-g95b7e673` spec — a `try.vikunja.io` *unstable* build 1019 commits past the `v2.3.0` tag — not the released tag. A tag-to-tag `v2.3.0`→`v2.4.0` diff finds **160 → 169 operations, nine added**: the eight `/admin/*` operations and `GET /projects/{project}/tasks/by-index/{index}`. All nine are counted `✅` here (`vikunja_admin`'s subcommands and `vikunja_tasks get-by-index`), so **nine implemented operations did not exist on a released Vikunja 2.3.0 — the floor as declared at the time.** This changed no count in the table above; it was a floor-policy exposure (do we still claim a 2.3.0 floor for those tools, gate them by detected server version, or raise the floor?). **RESOLVED 2026-08-31 — the floor was raised to 2.4.0** (§3 decision 27), the third of those three options: the denominator, the ✅ rows and the compatibility claim now all describe the same server surface, so 169 is an honest count against the floor as well as the aligned version. See the row 18 and row 19 amendments in §3. (2) **2.5.0 and 2.6.0 have not been vendored or diffed against these rows.** An external tag-to-tag comparison reports zero operations added 2.4.0→2.5.0 and exactly one added 2.5.0→2.6.0 (`DELETE /notifications`) — but the operation count is the wrong measure of that gap: the 2.4.0→2.6.0 delta for a client doing what we do is roughly 17 breaking / 21 behaviour-changing / 14 additive, almost entirely in handler enforcement the spec does not describe. Issue [#237](https://github.com/netadvanced/vikunja-mcp-ng/issues/237) carries the analysis and the alignment-target decision.

This re-verification found that most of the "implemented (bug)"/"partial" rows from the original audit had already been fixed by Wave B/C/D code changes but never re-graded in the coverage table — 8 of the 9 "implemented (bug)" rows and 2 of the 3 "partial" rows moved to ✅ on this pass, alongside 20 rows moving straight from ❌ to ✅ (project duplication, project views CRUD, Kanban bucket CRUD, direct user/team project sharing, and the export request/download tools, all of which existed in code but were undercounted). Only 1 ⚠️ row and 1 by-design 🟡 row remain; see `docs/API-COVERAGE.md`'s Issues table and Correctness Issues section for what's still genuinely open. This closes the "post-removal polish queue" re-grading item previously tracked here and in issue #28.

**Endpoint-tail re-triage (C2, 2026-07-18, decision 13).** All 64 `❌` operations were re-examined under the direct-REST architecture (`node-vikunja` removed) — the historical won't-implement list below predated that migration and several of its reasons no longer held (a broken client-library call; a blanket "binary/blob → nothing to expose" rule that had mislabeled JSON endpoints as blobs). New split: **20 IMPLEMENT (approved-to-build) / 36 PARKED / 8 NEVER**. At re-triage time this changed no coverage counts; as of **wave G-endpoint-tail (2026-07-18)** all 20 IMPLEMENT rows (candidates G1–G7) have since shipped and flipped `❌`→`✅` (64→44 `❌`, 103→123 `✅`), leaving the 36 PARKED and 8 NEVER rows as the remaining `❌`. Full per-operation verdicts, spec evidence, and the grouped backlog (G1–G7) are in **[docs/ENDPOINT-TAIL-RETRIAGE.md](ENDPOINT-TAIL-RETRIAGE.md)**; the flipped rows are annotated in `docs/API-COVERAGE.md`.

**Approved-to-build, shipped (20, were "won't-implement" or "not yet built" → IMPLEMENT → ✅, all landed in wave G-endpoint-tail, 2026-07-18):** CalDAV tokens (G1); task duplicate + mark-read (G2); the server-side user-export *status* read (G3, JSON `UserExportStatus`, not binary); user-level webhooks (G4, identical `models.Webhook` shape to project webhooks); avatar *provider* get/set + optional upload (G5, the provider endpoints are JSON, not image bytes); user deletion request/confirm/cancel (G6, behind the reserved deny-by-default `userDeletion` key + `confirm: true`); and the JSON subset of project backgrounds — remove / set-unsplash / unsplash-search (G7, low-priority/cosmetic). These 20 are the ✅ rows counted in the summary table above, not a separate backlog — see `docs/ENDPOINT-TAIL-RETRIAGE.md` for the per-operation verdicts.

**Won't implement — parked (36, feasible but deferred; each has a reopening trigger in the re-triage doc):**
- **Genuinely-binary image bytes** (project/Unsplash background *images* + thumbs, per-user avatar bytes, TOTP QR image): MCP has no binary content channel; a download-URL pattern is possible but very low value. (Note: the avatar/background *settings* and Unsplash *search* are JSON and were re-graded IMPLEMENT above — only the raw image bytes stay parked.)
- **Account-settings & session ceremonies that are feasible but governance-sensitive/low-value** (change password, change email, email confirm, one-time `/user/token`, server-side logout): deferred pending an explicit self-service account-settings module.
- **TOTP 2FA flows** (status/enroll/enable/disable + QR): an AI driving 2FA enrollment/teardown is a security anti-pattern; parked for a human-in-the-loop security-settings module.
- **Migration importers** (19 ops: CSV wizard + Todoist/Trello/Wekan/TickTick/MS-Todo/vikunja-file): one-shot administrative imports with OAuth ceremonies / file uploads — wrong tool for an MCP; the vikunja-file importer has the clearest latent value (round-trip our own `export.ts`).
- **`POST /tasks/{taskID}/assignees/bulk`** (general assign/unassign): replace-semantics footgun (silently unassigns everyone); the additive `PUT /assignees` loop is used deliberately for that flow (democratize-technology/vikunja-mcp#15). Reopens only if a true set-exact-assignees composite is wanted for general use. (Since decision 15: this same endpoint IS used, narrowly, by `vikunja_tasks bulk-update`'s post-bulk assignee-*restore* step, where replace semantics are safe because a complete snapshot is being restored — see decision 15 and `docs/API-COVERAGE.md`.)
- **`GET /routes`**: API self-listing with no AI task-management use case.

**Never (8, hard reason it makes no sense over MCP):** interactive `login`/`register`, OIDC callback, JWT `token/refresh` (needs a browser refresh-cookie this static-token client never holds — architecturally unreachable), the two anonymous pre-auth password-reset legs, and the two destructive test-only endpoints (`DELETE /test/all`, `PATCH /test/{table}`). Legitimate account provisioning remains `vikunja_admin create-user`; destructive account deletion is exposed once, `vikunja_admin`'s `delete-user`, gated behind `confirm: true` + deny-by-default + JWT-only.

## 5. Target tool surface

**27 registered top-level tools today** (re-counted 2026-08-31 from the tool names themselves, `grep -rhoE "'vikunja_[a-z_]+'" src/tools/ | sort -u`), ~150 subcommands across them. **18 register for any authenticated session** under the default config: `vikunja_auth`, `vikunja_tasks` + 6 task sub-resource tools (`vikunja_task_bulk`, `_assignees`, `_comments`, `_reminders`, `_labels`, `_relations`), `vikunja_projects`, `vikunja_labels`, `vikunja_teams`, `vikunja_filters`, `vikunja_templates`, `vikunja_webhooks`, `vikunja_batch_import`, `vikunja_notifications`, `vikunja_subscriptions`, `vikunja_reactions`. **The other 9 are JWT-only and/or deny-by-default:** `vikunja_users` and the four export tools (`vikunja_export_project`, `vikunja_request_user_export`, `vikunja_user_export_status`, `vikunja_download_user_export`), plus `vikunja_tokens`, `vikunja_caldav_tokens`, `vikunja_admin`, `vikunja_user_deletion` — see [TOOLS.md](TOOLS.md) for the same split at reference depth and [CONFIGURATION.md](CONFIGURATION.md#module-gating) for the gating mechanism. Most new capability arrives as *subcommands on existing tools*, not new tools — consolidation is deliberate: fewer, smarter tools cost less AI context and make module toggles meaningful (see §3b: this is the same axis the competitive review flagged as our clearest structural advantage over a flat 1:1-endpoint design). (OIDC mode, merged 2026-08-14, adds no new top-level tools — it changes *how* the existing 27 authenticate/isolate per-request, and adds `provision`/`status`/`deprovision` subcommands to the existing `vikunja_auth`; see [OIDC-RESOURCE-SERVER.md](OIDC-RESOURCE-SERVER.md) and [OIDC-SETUP.md](OIDC-SETUP.md).)

Remaining target surface (from the original ~55–60-operation Wave D plan, now delivered): saved filters, project sharing (link shares plus direct user/team sharing), project views/Kanban CRUD, project duplication, task extras (position/by-index/direct listing/subtask composites), attachments read-side, tokens/admin/info — **all landed**. Of the 64 operations not implemented before this wave (§4), the C2 re-triage (2026-07-18) found **20 approved-to-build** — grouped as wave items G1–G7 in [docs/ENDPOINT-TAIL-RETRIAGE.md](ENDPOINT-TAIL-RETRIAGE.md); those 20 shipped in **wave G-endpoint-tail (2026-07-18)**, most as subcommands on existing tools (`vikunja_tasks`, `vikunja_users`, `vikunja_projects`, `vikunja_webhooks`) plus two small new tools (`vikunja_caldav_tokens`, `vikunja_user_deletion`). The remaining 44 are parked/never per §4 (36 PARKED + 8 NEVER, re-verified 2026-07-21 per decision 19 with zero new surface added by the 2.4.0 alignment); all remaining work is tracked in issue #28.

## 6. Plan of record (waves)

| Wave | Scope | Status |
|---|---|---|
| A / B | Test-suite repair + 16 audit bug fixes (PRs #31–#41) | ✅ merged 2026-07-17 |
| A2 | Mop-up: remaining 68 failing tests → 0 (PRs #42–#46) | ✅ merged 2026-07-17 |
| C | Infrastructure: vendored spec + generated types; REST helper retry/breaker; module config + secrets; saga helper; endpoint playbook | ✅ merged 2026-07-17 |
| D | Capability build-out (3 batches) + full node-vikunja migration + final removal (PRs #55–#73) | ✅ merged 2026-07-18 — `node-vikunja` gone, grep-zero proven |
| E | Repackaging as `vikunja-mcp-ng`: Docker distribution, scenario README, samples, Docker Desktop MCP how-to (PR #74) | ✅ merged 2026-07-18 |
| F | Docs pass: roadmap rewrite, evaluated-options record, competitive-review snapshot, upstream courtesy report drafting, post-removal capability queue | ✅ merged 2026-07-18 |
| G-endpoint-tail | C2 re-triage's 20 approved-to-build ops (G1–G7: CalDAV tokens, task duplicate/mark-read, export status, user webhooks, avatar provider, user deletion, project backgrounds) | ✅ merged 2026-07-18 — coverage 103→123 ✅ |
| Release engineering | SemVer/changelog discipline, release scripts, `0.1.0`–`0.6.2` shipped, tag-triggered OIDC-Trusted-Publishing workflow installed | ✅ live since 2026-07-20; `0.6.2` shipped 2026-07-28 and is **still** npm `latest` (§2, decision 20) |
| A1 | Vikunja 2.4.0 alignment: pin bump, spec refresh, version-gated assignees-500 tolerance, proof matrix both DBs + floor | ✅ merged 2026-07-20 (decision 18) |
| T2 | 2.4.0 coverage re-audit: honest denominator, zero new surface vs. the vendored `v2.3.0-1019-g95b7e673` spec | ✅ merged 2026-07-21 (decision 19) — clarified 2026-08-31: that is a 2.4.0-surface denominator, not a released-2.3.0 one (§4, row 19 amendment) |
| T4 | Test-infra tightening: `spyOn`/`mockRestore` root cause, Web Storage warning noise | ✅ merged 2026-07-21 |
| `0.6.0` | Reliability + agent-ergonomics milestone: circuit-breaker cascade fix, silent date data-loss fix, Kanban/filter ergonomics, label attach-by-title, per-session v2/capability detection, multi-arch (`amd64`+`arm64`) GHCR images | ✅ shipped 2026-07-24 (decision 20) — npm `latest`, `CHANGELOG.md` |
| H1 + H2 (OIDC epic) | Multi-user HTTP/OIDC resource-server mode: transport, JWT validation, per-identity isolation, encrypted credential vault, self-service provisioning, mock-issuer e2e | ✅ feature-complete on `feat/oidc-mode` (2026-07-21) → **merged to `main` 2026-08-14** (#216), see the two rows below |
| `0.6.1` | Ergonomics/bugfix: `setup-kanban` composite (issue #173, PR #175), multi-task apply/remove-label (#178), bucket-order + fail-fast fixes (#176, #177), changelog-draft fix (#174) | ✅ shipped 2026-07-25 (decision 21) — one call instead of ~38, haiku 2/3 → 3/3 |
| `0.6.2` | Correctness: JWT-authenticated e2e lane (#198) and the multipart-through-JSON-helper bug it found (#199), `initialize` version derived from `package.json` (#186), `dist/` cleaned before build (#187), optional `columns` on `setup-kanban` (#185) | ✅ shipped 2026-07-28 |
| Release-pipeline repair | Native `ubuntu-24.04-arm` runners instead of QEMU, per-arch digest push + manifest merge, idempotent publish steps, `workflow_dispatch` recovery (#204) | ✅ merged 2026-07-28 — recovered `0.6.1`/`0.6.2`, which had reached npm with no image and no GitHub release |
| E2E stacks | One persistent stack per `<version>-<db>` target with derived ports and stable tokens; concurrency-safe `e2e:down` (#205, #206) | ✅ merged 2026-07-28 — `VIKUNJA_E2E_TARGET` now selects the stack (rough edges: #218, #219) |
| Prerelease pipeline | `-beta.x` tags publish to npm `beta` / GHCR `:beta` / a GitHub prerelease, never stealing `latest` (#214); transitive advisories cleared (#213) | ✅ merged 2026-08-12 — prerequisite for the OIDC beta |
| `0.7.0-beta.0` | Public beta of the OIDC resource-server mode: Streamable HTTP transport, per-user JWT identity + isolation, encrypted credential vault, RFC 9728 discovery, mock-issuer e2e lane, OIDC install manual (#216, #215, #212) | ✅ shipped 2026-08-14 — npm `beta`, GHCR `:beta`; `latest` untouched at `0.6.2` |
| `0.7.0-beta.1` | One-click SSO enrollment for oidc-http mode: `/enroll` + `/enroll/callback`, single-use identity-pinned tickets, server-side token minting into the vault, enrollment e2e lane (#220, #221) | ✅ shipped 2026-08-14 — open identity-pinning defects #223, #224 |
| Hardening batch (16 PRs) | Silent-field-drop class (#229, #236, #239, #240, #243), read-then-merge on full-replace endpoints (#230, #238), ambiguous-create retry ban (#228), date coercion on create paths (#242), filter-result honesty (#244), central log redaction (#241), BREAKING `percentDone` 0-100 (#231), battle library 13→21 (#235), docs currency (#232, #233, #234) | ✅ merged 2026-08-24 → 2026-08-31 — on `main`, **unreleased**; decisions 22–26 (§3) |

### Horizon (not yet scheduled)

- ~~**OIDC merge-back decision.**~~ **Closed: merged to `main` 2026-08-14** (#216, #221) and shipped as `0.7.0-beta.0`/`.1` on the npm `beta` tag — see §2 and the row 16 amendment in §3. What replaces it below is the *stable-release* question, not the merge question.
- **Graduating `0.7.0` from beta to stable — not scheduled.** `0.7.0-beta.1` has been on the `beta` dist-tag since 2026-08-14 and `latest` is still `0.6.2`, so **no published release carries either the OIDC mode or the 2026-08 hardening batch** (including the BREAKING `percentDone` change, decision 22). Two enrollment identity-pinning defects (#223, #224) are open against the beta, and the batch's changes are unreleased. Cutting `0.7.0` (or a further `-beta.2`) is an owner call; `docs/RELEASING.md` covers the mechanics.
- ~~**`0.6.1` — ergonomics/bugfix, scoped but not started.**~~ **Shipped 2026-07-25**, followed by `0.6.2` on 2026-07-28 — see §2 and the §6 table above.
- **v2 Vikunja API fast-path wave — re-slotted to `0.8.0` (issue [#184](https://github.com/netadvanced/vikunja-mcp-ng/issues/184)), not started.** *(The paragraph below is the 2026-08-03 record, kept for its trigger history; the version target it names is superseded — see the row 20 amendment in §3, and note the new pre-condition at the end.)* **Originally targeted `0.7.0` — not started; the wait is over (2026-08-03).** Decision 17 (§3) points at the v2 API's PATCH task route as the eventual replacement for the fetch-merge-POST/snapshot-restore workarounds (decisions 14/15); `0.6.0` vendored the v2 spec/types as prep (#147) and its own CHANGELOG names `0.7.0` as the migration release (decision 20). The start trigger was **either** roughly two weeks of additional upstream v2 field time from decision 17's writing (2026-07-20), **or** the next Vikunja point release (`2.4.1`), whichever came first. **The calendar arm elapsed on 2026-08-03**; no `2.4.1` has appeared (the e2e default pin and vendored spec are still `2.4.0`), so the trigger fired on time, not on a new upstream release. Scheduling the wave is now an owner call. Candidate migration list already exists from the companion investigation decision 17 references. **Added 2026-08-31:** re-verify before starting. v2 `PATCH` returns 422 on *subscribed* tasks (workaround: `subscription: null`), and Vikunja 2.6.0 makes task **creators** auto-subscribe — so a workaround scoped to assignees now has to hold for every task we create (#237). This is the item most likely to eat the milestone.
- **Vikunja 2.5.0 / 2.6.0 alignment — analysed, not scheduled (issue [#237](https://github.com/netadvanced/vikunja-mcp-ng/issues/237)).** Both are released upstream; we are aligned/tested at 2.4.0 with a 2.4.0 floor (raised from 2.3.0 on 2026-08-31) and claim nothing about either. #237 is deliberately framed as "here is what changes when we align", not "we are behind": three concrete exposures in our code (2.6.0's API-token scope check can 403 our `expand` passthrough for `tk_*` sessions — our *default* auth mode; task-position writes now verify the view belongs to the task's project, which our explicit-`projectViewId` path never checks; and the creator auto-subscribe risk to the v2 wave above), plus the alignment-target decision itself (move to 2.5.0, go straight to 2.6.0, or hold at 2.4.0 and raise only the floor policy), a spec re-vendor + operation-set diff, and standing up an e2e target for whichever version wins. Related, and **no longer open**: §4's floor-policy exposure — nine `✅` operations that do not exist on a released 2.3.0 — was resolved on 2026-08-31 by raising the floor to 2.4.0 (decision 27). One consequence lands here: floor and aligned now coincide, which collapses the pre-tag floor-regression lane, so whichever alignment target #237 picks should move `aligned` and re-separate them. **No timeline; not a blocker for a further beta, only for *claiming* 2.5/2.6 support.**
- **1.0.0 readiness — declare the tool surface stable after public soak (issue [#183](https://github.com/netadvanced/vikunja-mcp-ng/issues/183)).** Named here because it is already load-bearing on other decisions: decision 22's timing argument (a breaking `percentDone` change is cheap on `0.7.0-beta` and expensive after 1.0) only holds while this issue is open. The maintenance-branch model two bullets down is the other thing 1.0 switches on.
- **Parked backlog (36 items, each with its own reopening trigger).** No blanket timeline — every parked operation in §4 has a specific, concrete condition recorded in `docs/ENDPOINT-TAIL-RETRIAGE.md` (e.g. an explicit self-service account-settings module, a human-in-the-loop 2FA module, a true set-exact-assignees use case). Revisit individually as each trigger fires, not on a wave schedule.
- **Battle-scenario expansion — composites closed, library grown to 21 (2026-08-31), OIDC scenarios still open.** The composite gap this item was written for is gone, and #235 took the library from 13 to **21**: added teams (rename-keeps-visibility, create-with-admin-member), task position, three `percentDone` paths (update, bulk-update, filter threshold), and bulk-update partial failure, plus new check types (`team-exists`, `team-absent`, `task-absent-from-project`, `task-first-in-list-view`, a `max` on `tasks-with-label-count`), a `create-team` setup action, and team sweeping in cleanup (a team is global to the instance, so nothing else would reclaim it). Last full paid run was 13/13 on haiku at $0.4555 (2026-08-31, before the 8 new scenarios landed) with zero validation errors, retries or wrong-tool attempts; the 8 new scenarios have not yet had a paid run. What remains: **OIDC-mode isolation scenarios** (§3d of the OIDC design) — the merge-back that blocked them has landed, so they are now writable and merely unscheduled — and **re-deriving stale `optimalCallCount` baselines** (issue #202: still 7 of 13 scenarios beating their optimum, but not the same 7; #235 reports `bulk-priority-bump`'s optimum should be 2 rather than 3, since its baseline predates the columns-less `setup-kanban` from #185, and #236 re-derived `percent-done-scale`'s 2 → 1). Both remain **owner-approval-gated** before spending more paid-agent-run budget.
- **Maintenance branches for legacy majors (post-1.0 policy).** Once the project takes its first real major bump (`1.0 → 2.0` and beyond, in the SemVer-stable sense — not the pre-1.0 "declared stable" status change RELEASING.md §1 reserves today), the previous major becomes a long-lived **maintenance branch** rather than being retired outright. The driver is specific to this project: what keeps an old major alive is **Vikunja server compatibility** — self-hosters lag, so a frozen `1.x` line that still targets an older Vikunja server generation has real users long after `2.x` moves on to a newer one. An "EOL branch" here means "the branch that still supports an older Vikunja generation," and each major is effectively pinned to the server generation it was built and validated against (see the `-vikunja<ver>` compat aliases, §RELEASING.md 3). Model: security/maintenance fixes land on `main`, then cherry-pick to the handful of live maintenance branches; each cuts its own small release bumping **patch/minor only — never the frozen major**. Most of the pipeline already supports this: the release workflow triggers on *any* `v*` tag and builds from the tagged commit, so a `v1.6.1` tag pushed from a `1.x` branch publishes correctly (npm provenance, GHCR image, GH release) with no changes. **The one piece of real work the model adds is `latest`/dist-tag handling:** a maintenance release from a legacy branch must not steal npm `latest` or Docker `:latest` from the actively-developed line — the publish step would compute the dist-tag (take `latest` only when this version is the highest overall; otherwise publish under e.g. npm `--tag maintenance-1.x` and skip the Docker `:latest` tag). The per-branch compat aliases make this cleaner, since a legacy release's `-vikunja<ver>` tag already tells its users what it is independent of `latest`. Revisit at the 1.0 milestone; RELEASING.md §1's major-bump definition evolves at the same time.

## 7. Contributing / how work lands

Every change: feature branch → PR on `netadvanced/vikunja-mcp-ng` (never the upstream repo) → lint + typecheck + full test suite with a zero-net-new-regressions proof in the PR body → merge. Conventions for endpoint work live in `docs/ENDPOINT-PLAYBOOK.md`. The OpenAPI spec vendored at `docs/vikunja-openapi.json` is the only source of truth for API shapes — never `node-vikunja`'s types (the dependency itself is gone as of Wave D, but the principle stands for any future client-library temptation). Release mechanics (when/how a batch of merged PRs becomes a published version) are `docs/RELEASING.md`'s job, not this document's — this roadmap decides *what* and *why*, RELEASING.md executes *how*.

*Maintained by the project coordinator; update this file when decisions change, keeping §3's decision log append-only.*
